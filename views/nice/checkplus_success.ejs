<!--
/********************************************************************************************************************************************
		NICE평가정보 Copyright(c) KOREA INFOMATION SERVICE INC. ALL RIGHTS RESERVED
		
		서비스명 : 본인인증 (CheckPlus) 서비스
		페이지명 : 본인인증 (CheckPlus) 호출 페이지
*********************************************************************************************************************************************/
-->   
<html>
<head>
    
	<meta charset="utf-8">
    <title>NICE평가정보 - CheckPlus 본인인증 테스트</title>
    <%-include('../general/head') %>
</head>
<body>
    <form method="GET" id="infoForm">
        <input type="hidden" id="nextMethod" name="nextMethod" value="<%=nextMethod %>">
        <input type="hidden" id="name" name="name" value="<%=name %>">
        <input type="hidden" id="birthdate" name="userInfo" value="<%=birthdate %>">
        <input type="hidden" id="gender" name="gender" value="<%=gender %>">
        <input type="hidden" id="dupinfo" name="dupInfo" value="<%=dupinfo %>">
        <input type="hidden" id="mobileno" name="mobileNo" value="<%=mobileno %>">
        <input type="hidden" id="mobileco" value="<%=mobileco %>">
    </form>
    


    <center>
    <p><p><p><p>
    본인인증이 완료 되었습니다.<br>
    <table border=1>
        <tr>
            <td>message</td>
            <td id="sRtnMsg"><%=sRtnMSG%></td>
        </tr>
    </table>
    </center>
    <script>
        var userName = document.getElementById("name").value;
        var userInfo = document.getElementById("birthdate").value;
        var userGender = document.getElementById("gender").value;
        var dupInfo = document.getElementById("dupinfo").value;
        var userCellNo = document.getElementById("mobileno").value;
        var mobileCo = document.getElementById("mobileco").value;
        var nextMethod = document.getElementById("nextMethod").value;

        if (userGender == '1') {
            userGender = 'M'
        } else if ( userGender == '2'){
            userGender = 'W'
        }
        
        var params = {
            userName: userName,
            userInfo: userInfo,
            userGender: userGender,
            userCellNo: userCellNo,
            dupInfo: dupInfo
        }

        var sRtnMsg = $('#sRtnMsg').html();

        if (sRtnMsg == '') {
            opener.onIdentifyingSuccess(params, nextMethod);
            window.close();
        } else {
            alert('본인인증에 실패했습니다.');
        }
        
    </script>
</body>
</html>